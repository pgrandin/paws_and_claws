
// Physics
export const GRAVITY = -0.6; // Pulls down
export const JUMP_FORCE = 12; // Pushes up
export const BASE_SPEED = 7;
export const SPEED_INCREMENT = 0.001;
export const MAX_SPEED = 15;

// Dimensions (Logic units, mapped to % or pixels in render)
export const GAME_HEIGHT = 400;
export const GROUND_HEIGHT = 40; // The visual ground height
export const CAT_SIZE = 48; // Width/Height of cat
export const CAT_X_POSITION = 50; // Fixed X position of cat

// Obstacle Config
export const OBSTACLE_SPAWN_MIN_INTERVAL = 60; // Frames
export const OBSTACLE_SPAWN_MAX_INTERVAL = 140; // Frames
export const OBSTACLE_WIDTH = 40;
export const OBSTACLE_HEIGHT = 40;

// House Config
export const HOUSE_WIDTH = 120;
export const HOUSE_HEIGHT = 100;

// Rendering
export const PARALLAX_SPEED_FACTOR = 0.5;